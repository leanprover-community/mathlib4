#!/usr/bin/env bash

export LEAN_SYSROOT=$LEAN_REAL_SYSROOT

MODE=linking
for arg in "$@"; do
    if [[ "$arg" == "-c" ]]; then
        MODE=compilation
    fi
done
/usr/bin/time -f "\tnative $MODE %es" leanc "$@"
