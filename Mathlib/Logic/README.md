# Logic

This folder contains logic results that do not fit in either `ModelTheory` or `SetTheory`.

## TODO

Currently this folder contains a lot of basic results that should move to either `Basic` or `Order`.
