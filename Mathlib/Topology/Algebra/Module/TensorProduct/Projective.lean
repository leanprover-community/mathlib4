/-
Copyright (c) 2026 Michał Pacholski. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
Authors: Michał Pacholski
-/
module

public import Mathlib.Analysis.LocallyConvex.AbsConvex
public import Mathlib.LinearAlgebra.TensorProduct.Basic
public import Mathlib.Topology.Algebra.FilterBasis
/-!
# Projective topology on the tensor product of topological modules

This file defines the projective topology on the tensor product of two topological modules
over a commutative semiring, and establishes its properties when the scalar ring is a
nontrivially normed field.

The projective topology is defined as the finest locally convex topological module topology, for
which the canonical bilinear map is continuous. When working over a nontrivially normed field, we
construct this topology explicitly using a neighborhoos basis of zero made of absolutely convex
hulls.

## Main definitions

* `ProjectiveTensorProduct R M N` (notation: `M ⊗[R]π N`): A type synonym for the tensor product
  `M ⊗[R] N` equipped with the projective topology.
* `TensorProduct.projectiveTensorProductTopologies`: The set of all topologies on `M ⊗[R] N`
  making the space a locally convex topological module such that `tmul` is continuous.
* `TensorProduct.instTopologicalSpaceProjectiveTensorProduct`: The projective topology on
  `M ⊗[R]π N`, defined as the infimum of the topologies in `projectiveTensorProductTopologies R`.
* `TensorProduct.projectiveModuleFilterBasis`: The explicit `ModuleFilterBasis` on `E ⊗[𝕜] F`
  over a nontrivially normed field `𝕜`, generated by the absolutely convex hulls of the
  pointwise tensor products of neighborhood bases at zero.

## Main statements

* `TensorProduct.projectiveModuleFilterBasis_topology_mem_tensorProductTopologies`: The topology
  generated by the explicit filter basis makes the tensor product a locally convex topological
  module and the canonical bilinear map continuous.
* `TensorProduct.projectiveModuleFilterBasis_topology_eq_projectiveTopology`: The core equivalence
  theorem showing that the topology generated by the explicit filter basis is equal to the
  projective topology defined abstractly via the infimum.

## References

* H. H. Schaefer, M. P. Wolff, *Topological Vector Spaces*, Chapter III, Section 6 (pp. 92–97).
* https://en.wikipedia.org/wiki/Projective_tensor_product

## Tags

projective topology, tensor product, locally convex space, filter basis, absolutely convex hull
-/

@[expose] public section

open scoped TensorProduct Topology Pointwise
open Function Filter Bornology NormedField

section Semiring

variable {R M N : Type*}
variable [CommSemiring R] [AddCommMonoid M] [Module R M] [AddCommMonoid N] [Module R N]

variable (M N)

variable (R) in
/-- Type synonym for the projective tensor product of topological modules. -/
def ProjectiveTensorProduct := M ⊗[R] N

@[inherit_doc] scoped[TensorProduct] notation:100 M:100 " ⊗[" R "]π " N:101 =>
  ProjectiveTensorProduct R M N

instance : AddCommMonoid (M ⊗[R]π N) := TensorProduct.addCommMonoid
instance : Module R (M ⊗[R]π  N) := TensorProduct.instModule

end Semiring

namespace TensorProduct

section CommSemiring

variable {R M N : Type*}
variable [CommSemiring R] [TopologicalSpace R]
variable [PartialOrder R]
variable [AddCommGroup M] [Module R M] [TopologicalSpace M] [LocallyConvexSpace R M]
variable [AddCommGroup N] [Module R N] [TopologicalSpace N] [LocallyConvexSpace R N]

variable (R) in
/-- Set of all locally convex topological module topology,
for which the canonical bilinear map is continuous -/
def tensorProductTopologies := { t : TopologicalSpace (M ⊗[R] N) |
    letI := t
    IsTopologicalAddGroup (M ⊗[R] N) ∧
    LocallyConvexSpace R (M ⊗[R] N) ∧
    ContinuousSMul R (M ⊗[R] N) ∧
    Continuous (Function.uncurry (tmul R : M → N → M ⊗[R] N))
}

/-- The projective topology on `X ⊗[𝕜]π Y` is defined as the infimum of all topologies
making the space a locally convex topological module such that `tmul` is continuous. -/
instance instTopologicalSpaceProjectiveTensorProduct : TopologicalSpace (M ⊗[R]π N) :=
  sInf (tensorProductTopologies R)

instance : LocallyConvexSpace R (M ⊗[R]π N) := LocallyConvexSpace.sInf fun _ ⟨_, h, _⟩ ↦ h

end CommSemiring
section NontriviallyNormedField

variable {𝕜 E F : Type*}
variable [NontriviallyNormedField 𝕜]
variable [AddCommGroup E] [Module 𝕜 E]
variable [AddCommGroup F] [Module 𝕜 F]
variable {𝔘 : Set (Set E)} {𝔙 : Set (Set F)}

section PartialOrder

variable [PartialOrder 𝕜]

/-- Given collections of sets `𝔘` in `E` and `𝔙` in `F`, `projectiveModuleFilterBasisSets 𝔘 𝔙`
is the collection of absolutely convex hulls of the set-wise tensor products `U ⊗ˢ[𝕜] V` for
`U ∈ 𝔘` and `V ∈ 𝔙`. When `𝔘` and `𝔙` constitute neighborhood bases of zero in `E` and `F`
respectively, this collection forms a neighborhood basis of zero for the projective tensor product
topology. -/
def projectiveModuleFilterBasisSets (𝔘 : Set (Set E)) (𝔙 : Set (Set F)) :=
  { s | ∃ U ∈ 𝔘, ∃ V ∈ 𝔙, s = absConvexHull 𝕜 (U ⊗ˢ[𝕜] V) }

lemma projectiveModuleFilterBasisSets_neg
    {W : Set (E ⊗[𝕜] F)} (hW : W ∈ projectiveModuleFilterBasisSets 𝔘 𝔙) :
    ∃ t ∈ projectiveModuleFilterBasisSets 𝔘 𝔙, t ⊆ -W := by
  use W
  constructor
  · exact hW
  · obtain ⟨U, _, V, _, rfl⟩ := hW
    have h_balanced : Balanced 𝕜 (absConvexHull 𝕜 (U ⊗ˢ[𝕜] V)) := balanced_absConvexHull
    have h_subset := h_balanced (-1 : 𝕜) (by norm_num)
    rintro x hx
    simpa using h_subset ⟨x, hx, rfl⟩

lemma projectiveModuleFilterBasisSets_smul
    {W : Set (E ⊗[𝕜] F)} (hW : W ∈ projectiveModuleFilterBasisSets 𝔘 𝔙) :
    ∃ U ∈ 𝓝 (0 : 𝕜), ∃ t ∈ projectiveModuleFilterBasisSets 𝔘 𝔙, U • t ⊆ W := by
  use Metric.closedBall (0 : 𝕜) 1
  constructor
  · exact Metric.closedBall_mem_nhds 0 zero_lt_one
  · use W
    constructor
    · exact hW
    · rintro _ ⟨c, hc, x, hx, rfl⟩
      obtain ⟨U, hU, V, hV, rfl⟩ := hW
      have h_balanced : Balanced 𝕜 (absConvexHull 𝕜 (U ⊗ˢ[𝕜] V)) := balanced_absConvexHull
      have hc_norm : ‖c‖ ≤ 1 := by simpa using hc
      apply h_balanced c hc_norm ⟨x, hx, rfl⟩

variable [TopologicalSpace E] [TopologicalSpace F]

lemma projectiveModuleFilterBasisSets_nonempty
    (h𝔘 : (𝓝 (0 : E)).HasBasis 𝔘 id) (h𝔙 : (𝓝 (0 : F)).HasBasis 𝔙 id) :
    (projectiveModuleFilterBasisSets 𝔘 𝔙 : Set (Set (E ⊗[𝕜] F))).Nonempty := by
  rcases h𝔘.ex_mem with ⟨U, hU⟩
  rcases h𝔙.ex_mem with ⟨V, hV⟩
  exact ⟨absConvexHull 𝕜 (U ⊗ˢ[𝕜] V), U, hU, V, hV, rfl⟩

lemma projectiveModuleFilterBasisSets_inter_sets
    (h𝔘 : (𝓝 (0 : E)).HasBasis 𝔘 id) (h𝔙 : (𝓝 (0 : F)).HasBasis 𝔙 id)
    {x y : Set (E ⊗[𝕜] F)} (hx : x ∈ projectiveModuleFilterBasisSets 𝔘 𝔙)
    (hy : y ∈ projectiveModuleFilterBasisSets 𝔘 𝔙) :
    ∃ z ∈ projectiveModuleFilterBasisSets 𝔘 𝔙, z ⊆ x ∩ y := by
  obtain ⟨U₁, hU₁, V₁, hV₁, rfl⟩ := hx
  obtain ⟨U₂, hU₂, V₂, hV₂, rfl⟩ := hy
  obtain ⟨U₃, hU₃, hU₃_sub⟩ := h𝔘.mem_iff.mp (inter_mem (h𝔘.mem_of_mem hU₁) (h𝔘.mem_of_mem hU₂))
  obtain ⟨V₃, hV₃, hV₃_sub⟩ := h𝔙.mem_iff.mp (inter_mem (h𝔙.mem_of_mem hV₁) (h𝔙.mem_of_mem hV₂))
  use absConvexHull 𝕜 (U₃ ⊗ˢ[𝕜] V₃), ⟨U₃, hU₃, V₃, hV₃, rfl⟩
  refine absConvexHull_min ?_ (AbsConvex.inter absConvex_absConvexHull absConvex_absConvexHull)
  rintro _ ⟨u, hu, v, hv, rfl⟩
  exact ⟨subset_absConvexHull ⟨u, (hU₃_sub hu).1, v, (hV₃_sub hv).1, rfl⟩,
         subset_absConvexHull ⟨u, (hU₃_sub hu).2, v, (hV₃_sub hv).2, rfl⟩⟩

lemma projectiveModuleFilterBasisSets_zero
    (h𝔘 : (𝓝 (0 : E)).HasBasis 𝔘 id) (h𝔙 : (𝓝 (0 : F)).HasBasis 𝔙 id)
    {s : Set (E ⊗[𝕜] F)} (hs : s ∈ projectiveModuleFilterBasisSets 𝔘 𝔙) :
    (0 : E ⊗[𝕜] F) ∈ s := by
  obtain ⟨U, hU, V, hV, rfl⟩ := hs
  have hU_zero : (0 : E) ∈ U := mem_of_mem_nhds (h𝔘.mem_of_mem hU)
  have hV_zero : (0 : F) ∈ V := mem_of_mem_nhds (h𝔙.mem_of_mem hV)
  have h_zero_tmul : (0 : E) ⊗ₜ[𝕜] (0 : F) ∈ U ⊗ˢ[𝕜] V := Set.mem_image2_of_mem hU_zero hV_zero
  simpa [zero_tmul] using subset_absConvexHull h_zero_tmul

variable [ContinuousSMul 𝕜 E]

omit [TopologicalSpace F] in
lemma projectiveModuleFilterBasisSets_smul_left (h𝔘 : (𝓝 (0 : E)).HasBasis 𝔘 id)
    (c : 𝕜) {W : Set (E ⊗[𝕜] F)} (hW : W ∈ projectiveModuleFilterBasisSets 𝔘 𝔙) :
    ∃ t ∈ projectiveModuleFilterBasisSets 𝔘 𝔙, t ⊆ (fun x ↦ c • x) ⁻¹' W := by
  obtain ⟨U, hU, V, hV, rfl⟩ := hW
  have hcU_nhds : (fun x ↦ c • x) ⁻¹' U ∈ 𝓝 0 :=
    (continuous_const_smul c).tendsto' 0 0 (smul_zero c) (h𝔘.mem_of_mem hU)
  obtain ⟨U', hU', hU_sub⟩ := h𝔘.mem_iff.mp hcU_nhds
  use absConvexHull 𝕜 (U' ⊗ˢ[𝕜] V), ⟨U', hU', V, hV, rfl⟩
  have h_abs : AbsConvex 𝕜 ((fun x ↦ c • x) ⁻¹' (absConvexHull 𝕜 (U ⊗ˢ[𝕜] V))) := by
    refine ⟨fun a ha x hx ↦ ?_, convex_absConvexHull.smul_preimage c⟩
    obtain ⟨y, hy, rfl⟩ := Set.mem_smul_set.mp hx
    rw [Set.mem_preimage] at hy ⊢
    rw [smul_comm]
    exact balanced_absConvexHull.smul_mem ha hy
  refine absConvexHull_min ?_ h_abs
  rintro _ ⟨u, hu, v, hv, rfl⟩
  exact subset_absConvexHull ⟨c • u, hU_sub hu, v, hv, rfl⟩

end PartialOrder

section LinearOrder

variable [LinearOrder 𝕜] [IsStrictOrderedRing 𝕜]
variable [TopologicalSpace E] [ContinuousSMul 𝕜 E]
variable [TopologicalSpace F]

lemma projectiveModuleFilterBasisSets_add_subset
    (h𝔘 : (𝓝 (0 : E)).HasBasis 𝔘 id) (_ : (𝓝 (0 : F)).HasBasis 𝔙 id)
    {W : Set (E ⊗[𝕜] F)} (hW : W ∈ projectiveModuleFilterBasisSets 𝔘 𝔙) :
    ∃ W' ∈ projectiveModuleFilterBasisSets 𝔘 𝔙, W' + W' ⊆ W := by
  obtain ⟨U, hU, V, hV, rfl⟩ := hW
  obtain ⟨c, hc, hc_lt⟩ : ∃ (c : 𝕜), 0 < ‖c‖ ∧ ‖c‖ < 1/2 := exists_norm_lt 𝕜 (by norm_num)
  have hc_ne : c ≠ 0 := norm_pos_iff.mp hc
  have h_cont := (continuous_const_smul c⁻¹).tendsto (0 : E)
  rw [smul_zero] at h_cont
  obtain ⟨U', hU', hU'_sub⟩ := h𝔘.mem_iff.mp (h_cont (h𝔘.mem_iff.mpr ⟨U, hU, subset_rfl⟩))
  use absConvexHull 𝕜 (U' ⊗ˢ[𝕜] V), ⟨U', hU', V, hV, rfl⟩
  have h_sub : U' ⊗ˢ[𝕜] V ⊆ c • (U ⊗ˢ[𝕜] V) := by
    rintro _ ⟨x, hx, y, hy, rfl⟩
    refine ⟨(c⁻¹ • x) ⊗ₜ[𝕜] y, ⟨c⁻¹ • x, hU'_sub hx, y, hy, rfl⟩, ?_⟩
    simp [smul_tmul, smul_smul, mul_inv_cancel₀ hc_ne]
  have h_t_sub : absConvexHull 𝕜 (U' ⊗ˢ[𝕜] V) ⊆ c • absConvexHull 𝕜 (U ⊗ˢ[𝕜] V) :=
    absConvexHull_min (h_sub.trans (Set.image_mono subset_absConvexHull))
      ⟨balanced_absConvexHull.smul c, convex_absConvexHull.smul c⟩
  refine (Set.add_subset_add h_t_sub h_t_sub).trans ?_
  rintro _ ⟨_, ⟨y1, hy1, rfl⟩, _, ⟨y2, hy2, rfl⟩, rfl⟩
  change c • y1 + c • y2 ∈ _
  have h_scalar : (2 * c) * (1 / 2 : 𝕜) = c := by
    rw [mul_comm (2 : 𝕜), mul_assoc, mul_one_div, div_self (by norm_num), mul_one]
  have eq : c • y1 + c • y2 = (2 * c) • ((1 / 2 : 𝕜) • y1 + (1 / 2 : 𝕜) • y2) := by
    rw [smul_add, smul_smul, h_scalar, smul_smul, h_scalar]
  rw [eq]
  have h_two : ‖(2 : 𝕜)‖ ≤ 2 := by
    rw [show (2 : 𝕜) = 1 + 1 by ring]
    exact (norm_add_le 1 1).trans_eq (by norm_num)
  have h_norm_le : ‖(2 : 𝕜) * c‖ ≤ 1 := by
    calc
      ‖(2 : 𝕜) * c‖ = ‖(2 : 𝕜)‖ * ‖c‖ := norm_mul _ _
      _ ≤ 2 * ‖c‖ := mul_le_mul_of_nonneg_right h_two (norm_nonneg _)
      _ ≤ 1 := by linarith [hc_lt]
  exact balanced_absConvexHull.smul_mem h_norm_le
    (convex_absConvexHull hy1 hy2 (by positivity) (by positivity) (add_halves 1))

variable [ContinuousSMul 𝕜 F]
variable (h𝔘 : (𝓝 (0 : E)).HasBasis 𝔘 id) (h𝔙 : (𝓝 (0 : F)).HasBasis 𝔙 id)

lemma projectiveModuleFilterBasisSets_smul_right
    (h𝔘 : (𝓝 (0 : E)).HasBasis 𝔘 id) (h𝔙 : (𝓝 (0 : F)).HasBasis 𝔙 id) (z : E ⊗[𝕜] F) :
    ∀ {W : Set (E ⊗[𝕜] F)}, W ∈ projectiveModuleFilterBasisSets 𝔘 𝔙 → ∀ᶠ c in 𝓝 (0 : 𝕜),
    c • z ∈ W := by
  induction z using TensorProduct.induction_on with
  | zero =>
    intro W hW
    filter_upwards with c
    rw [smul_zero]
    exact projectiveModuleFilterBasisSets_zero h𝔘 h𝔙 hW
  | tmul u v =>
    intro W hW
    obtain ⟨U, hU, V, hV, rfl⟩ := hW
    have hV_nhds : V ∈ 𝓝 0 := h𝔙.mem_iff.mpr ⟨V, hV, subset_rfl⟩
    rcases exists_smul_of_mem_nhds_zero (𝕜 := 𝕜) hV_nhds v with ⟨r, _, v', hv', rfl⟩
    have h_ev : ∀ᶠ c in 𝓝 0, (c * r) • u ∈ U := by
      have ht : Tendsto (fun c : 𝕜 ↦ (c * r) • u) (𝓝 0) (𝓝 0) :=
        (continuous_id.mul continuous_const).smul continuous_const |>.tendsto' 0 0 (by simp)
      exact ht (h𝔘.mem_iff.mpr ⟨U, hU, subset_rfl⟩)
    filter_upwards [h_ev] with c hc
    have : c • (u ⊗ₜ[𝕜] (r • v')) = ((c * r) • u) ⊗ₜ[𝕜] v' := by
      simp [smul_tmul, tmul_smul, smul_smul]
    rw [this]
    exact subset_absConvexHull (Set.mem_image2_of_mem hc hv')
  | add x y hx hy =>
    intro W hW
    obtain ⟨W', hW', h_subset⟩ := projectiveModuleFilterBasisSets_add_subset h𝔘 h𝔙 hW
    filter_upwards [hx hW', hy hW'] with c hcx hcy
    rw [smul_add]
    exact h_subset (Set.add_mem_add hcx hcy)

/-- Constructs the `ModuleFilterBasis` on the tensor product `E ⊗[𝕜] F` given neighborhood
bases `𝔘` of zero in `E` and `𝔙` of zero in `F`. The underlying filter basis sets are the
absolutely convex hulls of `U ⊗ˢ[𝕜] V`. This basis induces the projective topology,
equipping the tensor product with a topological module structure. -/
def projectiveModuleFilterBasis : ModuleFilterBasis 𝕜 (E ⊗[𝕜] F) where
  sets := projectiveModuleFilterBasisSets 𝔘 𝔙
  nonempty := projectiveModuleFilterBasisSets_nonempty h𝔘 h𝔙
  inter_sets := by
    rintro x y hx hy
    exact projectiveModuleFilterBasisSets_inter_sets h𝔘 h𝔙 hx hy
  zero' := by
    rintro s hs
    exact projectiveModuleFilterBasisSets_zero h𝔘 h𝔙 hs
  add' := by
    rintro s hs
    exact projectiveModuleFilterBasisSets_add_subset h𝔘 h𝔙 hs
  neg' := by
    rintro s hs
    exact projectiveModuleFilterBasisSets_neg hs
  conj' := by
    rintro _ s hs
    exact ⟨s, hs, by simp⟩
  smul' := by
    rintro s hs
    exact projectiveModuleFilterBasisSets_smul hs
  smul_left' := by
    rintro c s hs
    exact projectiveModuleFilterBasisSets_smul_left h𝔘 c hs
  smul_right' := by
    rintro x s hs
    exact projectiveModuleFilterBasisSets_smul_right h𝔘 h𝔙 x hs

lemma locallyConvexSpace_projectiveModuleFilterBasis :
    @LocallyConvexSpace 𝕜 (E ⊗[𝕜] F) _ _ _ _ (projectiveModuleFilterBasis h𝔘 h𝔙).topology := by
  letI topology := (projectiveModuleFilterBasis h𝔘 h𝔙).topology (R:=𝕜)
  apply LocallyConvexSpace.ofBasisZero 𝕜 (E ⊗[𝕜] F) id _
    (projectiveModuleFilterBasis h𝔘 h𝔙).nhds_zero_hasBasis
  rintro s ⟨U, hU, V, hV, rfl⟩
  exact convex_absConvexHull

lemma tendsto_tmul_nhds_zero_projectiveModuleFilterBasis :
    Tendsto (fun p : E × F ↦ p.1 ⊗ₜ[𝕜] p.2) (𝓝 0 ×ˢ 𝓝 0)
    (projectiveModuleFilterBasis h𝔘 h𝔙).filter := by
  rw [(projectiveModuleFilterBasis h𝔘 h𝔙).toFilterBasis.hasBasis.tendsto_right_iff]
  rintro s ⟨U, hU, V, hV, rfl⟩
  have hb_nhds : U ∈ 𝓝 (0 : E) := h𝔘.mem_of_mem hU
  have hb'_nhds : V ∈ 𝓝 (0 : F) := h𝔙.mem_of_mem hV
  have h_prod : U ×ˢ V ∈ 𝓝 (0 : E) ×ˢ 𝓝 (0 : F) := prod_mem_prod hb_nhds hb'_nhds
  filter_upwards [h_prod]
  rintro ⟨x, y⟩ ⟨hx : x ∈ U, hy : y ∈ V⟩
  exact subset_absConvexHull (Set.mem_image2_of_mem hx hy)

lemma continuousAt_tmul_right_projectiveModuleFilterBasis (y : F) : @ContinuousAt E (E ⊗[𝕜] F) _
    (projectiveModuleFilterBasis h𝔘 h𝔙).topology (fun x ↦ x ⊗ₜ[𝕜] y) 0 := by
  letI topology := (projectiveModuleFilterBasis h𝔘 h𝔙).topology (R:=𝕜)
  rw [ContinuousAt, zero_tmul]
  rw
    [(projectiveModuleFilterBasis h𝔘 h𝔙).toAddGroupFilterBasis.nhds_zero_hasBasis.tendsto_right_iff]
  rintro s ⟨U, hU, V, hV, rfl⟩
  have hb'_nhds : V ∈ 𝓝 (0 : F) := h𝔙.mem_of_mem hV
  rcases exists_smul_of_mem_nhds_zero (𝕜 := 𝕜) hb'_nhds y with ⟨r, hr_ne, y', hy', rfl⟩
  have hb_nhds : U ∈ 𝓝 (0 : E) := h𝔘.mem_of_mem hU
  have h_smul : ∀ᶠ x : E in 𝓝 0, r • x ∈ U :=
    (continuous_const_smul r).continuousAt (by simpa using hb_nhds)
  filter_upwards [h_smul] with x hx
  rw [← TensorProduct.smul_tmul]
  exact subset_absConvexHull (Set.mem_image2_of_mem hx hy')

lemma continuousAt_mk_apply_projectiveModuleFilterBasis (x : E) :
    @ContinuousAt F (E ⊗[𝕜] F) _ (projectiveModuleFilterBasis h𝔘 h𝔙).topology ((mk 𝕜 E F) x) 0 := by
  letI topology := (projectiveModuleFilterBasis h𝔘 h𝔙).topology (R:=𝕜)
  rw [ContinuousAt, map_zero]
  rw [(projectiveModuleFilterBasis h𝔘 h𝔙).nhds_zero_hasBasis.tendsto_right_iff]
  rintro s ⟨U, hU, V, hV, rfl⟩
  have hb_nhds : U ∈ 𝓝 (0 : E) := h𝔘.mem_of_mem hU
  rcases exists_smul_of_mem_nhds_zero (𝕜 := 𝕜) hb_nhds x with ⟨r, hr_ne, x', hx', rfl⟩
  have hb'_nhds : V ∈ 𝓝 (0 : F) := h𝔙.mem_of_mem hV
  have h_smul : ∀ᶠ y : F in 𝓝 0, r • y ∈ V :=
    (continuous_const_smul r).continuousAt (by simpa using hb'_nhds)
  filter_upwards [h_smul] with y hy
  rw [mk_apply, smul_tmul]
  exact subset_absConvexHull (Set.mem_image2_of_mem hx' hy)

variable [IsTopologicalAddGroup E] [IsTopologicalAddGroup F]

theorem projectiveModuleFilterBasis_topology_mem_tensorProductTopologies :
    (projectiveModuleFilterBasis h𝔘 h𝔙).topology ∈ tensorProductTopologies 𝕜 := by
  letI topology := (projectiveModuleFilterBasis h𝔘 h𝔙).topology (R:=𝕜)
  unfold tensorProductTopologies
  simp only [Set.mem_ofPred_eq]
  refine ⟨(projectiveModuleFilterBasis h𝔘 h𝔙).isTopologicalAddGroup,
    locallyConvexSpace_projectiveModuleFilterBasis h𝔘 h𝔙,
    (projectiveModuleFilterBasis h𝔘 h𝔙).continuousSMul, ?_⟩
  apply continuous_of_continuousAt_zero₂
    (LinearMap.toAddMonoidHom'.comp (TensorProduct.mk 𝕜 E F).toAddMonoidHom)
  · rw [ContinuousAt, Prod.mk_zero_zero]
    simp only [AddMonoidHom.coe_comp, LinearMap.toAddMonoidHom_coe, comp_apply,
      LinearMap.toAddMonoidHom'_apply, mk_apply, Prod.fst_zero, map_zero, Prod.snd_zero]
    rw [nhds_prod_eq, (projectiveModuleFilterBasis h𝔘 h𝔙).toAddGroupFilterBasis.nhds_zero_eq]
    exact tendsto_tmul_nhds_zero_projectiveModuleFilterBasis (𝕜 := 𝕜) h𝔘 h𝔙
  · simp only [AddMonoidHom.coe_comp, LinearMap.toAddMonoidHom_coe, comp_apply,
      LinearMap.toAddMonoidHom'_apply]
    exact continuousAt_mk_apply_projectiveModuleFilterBasis h𝔘 h𝔙
  · simp only [AddMonoidHom.coe_comp, LinearMap.toAddMonoidHom_coe, comp_apply,
      LinearMap.toAddMonoidHom'_apply, mk_apply]
    exact continuousAt_tmul_right_projectiveModuleFilterBasis h𝔘 h𝔙

theorem projectiveModuleFilterBasis_topology_eq_projectiveTopology :
    (projectiveModuleFilterBasis h𝔘 h𝔙).topology = (instTopologicalSpaceProjectiveTensorProduct
      (R:=𝕜) (M:=E) (N:=F)) := by
  apply le_antisymm
  · apply le_sInf
    intro t ⟨ht_isTopologicalAddGroup, ht_locallyConvexSpace, ht_continuousSMul , ht_cont_tmul⟩
    letI := t
    let t_hasBasis_absConvex := nhds_hasBasis_absConvex 𝕜 (E ⊗[𝕜] F)
    apply IsTopologicalAddGroup.le_of_nhds_zero_le_nhds_zero
    rw [(projectiveModuleFilterBasis h𝔘 h𝔙).nhds_zero_hasBasis.le_basis_iff t_hasBasis_absConvex]
    intro W ⟨hW_mem_nhds_zero, hW_absConvex⟩
    let W' := (Function.uncurry (tmul 𝕜)) ⁻¹' W
    have hW'_nhds : W' ∈ 𝓝 (0, 0) :=
      ht_cont_tmul.continuousAt.preimage_mem_nhds (by simpa using hW_mem_nhds_zero)
    rw [nhds_prod_eq, (h𝔘.prod h𝔙).mem_iff] at hW'_nhds
    obtain ⟨⟨U, V⟩, ⟨⟨hU, hV⟩, hW'⟩⟩ := hW'_nhds
    have h_tmul_subset_W : U ⊗ˢ[𝕜] V ⊆ W := by
      rw [Set.prod_subset_iff] at hW'
      rintro _ ⟨u, hu, v, hv, rfl⟩
      exact hW' u hu v hv
    use absConvexHull 𝕜 (U ⊗ˢ[𝕜] V)
    refine ⟨⟨U, hU, V, hV, rfl⟩,
      (AbsConvex.absConvexHull_subset_iff hW_absConvex).mpr h_tmul_subset_W⟩
  · apply sInf_le
    exact projectiveModuleFilterBasis_topology_mem_tensorProductTopologies h𝔘 h𝔙

end LinearOrder

end NontriviallyNormedField

end TensorProduct
