/-
Copyright (c) 2017 Johannes Hölzl. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
Authors: Johannes Hölzl, Kim Morrison
-/
module

public import Mathlib.Algebra.Group.Finsupp
public import Mathlib.Algebra.Group.Submonoid.Basic
public import Mathlib.Algebra.Group.TypeTags.Hom

/-!
# Extensionality for maps on `Finsupp`

This file contains some extensionality principles for maps on `Finsupp`.
These have been moved to their own file to avoid depending on submonoids when defining `Finsupp`.

## Main results

* `Finsupp.mem_addSubmonoid_closure_setOf_single_iff`: the additive submonoid generated by the
  `single a y` with `a ∈ s` and `y` in a generating set of `M` consists of the functions
  supported in `s`
* `Finsupp.add_closure_setOfPred_eq_single`: `Finsupp` is generated by all the `single`s
* `Finsupp.addHom_ext`: additive homomorphisms that are equal on each `single` are equal everywhere
-/

public section

variable {α M N : Type*}

namespace Finsupp

variable [AddZeroClass M]

/-- The additive submonoid of `α →₀ M` generated by the `single a y` with `a ∈ s` and `y` in a
generating set `G` of `M` consists exactly of the functions supported in `s`.

`Finsupp.add_closure_setOfPred_eq_single` is the case `s = Set.univ`, `G = Set.univ`, and
`Finsupp.mem_addSubgroup_closure_setOf_single_iff` is the analogue for `AddSubgroup.closure`. -/
theorem mem_addSubmonoid_closure_setOf_single_iff {G : Set M} {s : Set α}
    (hG : AddSubmonoid.closure G = ⊤) {f : α →₀ M} :
    f ∈ AddSubmonoid.closure {g : α →₀ M | ∃ a ∈ s, ∃ y ∈ G, g = single a y} ↔
      ↑f.support ⊆ s := by
  classical
  constructor
  · intro hf
    induction hf using AddSubmonoid.closure_induction with
    | mem g hg =>
      obtain ⟨a, ha, y, -, rfl⟩ := hg
      intro x hx
      rw [Finset.mem_singleton.1 (support_single_subset (Finset.mem_coe.1 hx))]
      exact ha
    | zero => simp
    | add g h _ _ hg hh =>
      refine (Finset.coe_subset.2 support_add).trans ?_
      rw [Finset.coe_union]
      exact Set.union_subset hg hh
  · have hsingle : ∀ a ∈ s, ∀ y : M,
        single a y ∈ AddSubmonoid.closure {g : α →₀ M | ∃ a ∈ s, ∃ y ∈ G, g = single a y} := by
      intro a ha y
      have hy : y ∈ AddSubmonoid.closure G := hG ▸ AddSubmonoid.mem_top y
      induction hy using AddSubmonoid.closure_induction with
      | mem z hz => exact AddSubmonoid.subset_closure ⟨a, ha, z, hz, rfl⟩
      | zero => simp
      | add z w _ _ hz hw => rw [single_add]; exact add_mem hz hw
    induction f using Finsupp.induction₂ with
    | zero => exact fun _ ↦ zero_mem _
    | add_single a y g ha hy ih =>
      intro hsub
      rw [support_add_eq (by rw [support_single _ hy]; simpa using ha), support_single _ hy,
        Finset.coe_union, Set.union_subset_iff, Finset.coe_singleton,
        Set.singleton_subset_iff] at hsub
      exact add_mem (ih hsub.1) (hsingle a hsub.2 y)

@[simp]
theorem add_closure_setOfPred_eq_single :
    AddSubmonoid.closure { f : α →₀ M | ∃ a b, f = single a b } = ⊤ := by
  refine top_unique fun x _ ↦ AddSubmonoid.closure_mono ?_
    ((mem_addSubmonoid_closure_setOf_single_iff (s := Set.univ) (G := Set.univ)
      AddSubmonoid.closure_univ).2 (Set.subset_univ _))
  rintro g ⟨a, -, b, -, rfl⟩
  exact ⟨a, b, rfl⟩

@[deprecated (since := "2026-07-09")]
alias add_closure_setOf_eq_single := add_closure_setOfPred_eq_single

/-- If two additive homomorphisms from `α →₀ M` are equal on each `single a b`,
then they are equal. -/
theorem addHom_ext [AddZeroClass N] ⦃f g : (α →₀ M) →+ N⦄
    (H : ∀ x y, f (single x y) = g (single x y)) : f = g := by
  refine AddMonoidHom.eq_of_eqOn_denseM add_closure_setOfPred_eq_single ?_
  rintro _ ⟨x, y, rfl⟩
  apply H

/-- If two additive homomorphisms from `α →₀ M` are equal on each `single a b`,
then they are equal.

We formulate this using equality of `AddMonoidHom`s so that `ext` tactic can apply a type-specific
extensionality lemma after this one.  E.g., if the fiber `M` is `ℕ` or `ℤ`, then it suffices to
verify `f (single a 1) = g (single a 1)`. -/
@[ext high]
theorem addHom_ext' [AddZeroClass N] ⦃f g : (α →₀ M) →+ N⦄
    (H : ∀ x, f.comp (singleAddHom x) = g.comp (singleAddHom x)) : f = g :=
  addHom_ext fun x => DFunLike.congr_fun (H x)

theorem mulHom_ext [MulOneClass N] ⦃f g : Multiplicative (α →₀ M) →* N⦄
    (H : ∀ x y, f (Multiplicative.ofAdd <| single x y) = g (Multiplicative.ofAdd <| single x y)) :
    f = g :=
  MonoidHom.ext <|
    DFunLike.congr_fun <| by
      have := addHom_ext (f := f.toAdditiveRight) (g := g.toAdditiveRight) H
      ext
      rw [DFunLike.ext_iff] at this
      apply this

@[ext]
theorem mulHom_ext' [MulOneClass N] {f g : Multiplicative (α →₀ M) →* N}
    (H : ∀ x, f.comp (AddMonoidHom.toMultiplicative (singleAddHom x)) =
              g.comp (AddMonoidHom.toMultiplicative (singleAddHom x))) :
    f = g :=
  mulHom_ext fun x => DFunLike.congr_fun (H x)

end Finsupp
