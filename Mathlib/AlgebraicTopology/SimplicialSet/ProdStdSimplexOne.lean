/-
Copyright (c) 2025 Joël Riou. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
Authors: Joël Riou
-/
module

public import Mathlib.AlgebraicTopology.SimplicialSet.ProdStdSimplex
public import Mathlib.AlgebraicTopology.SimplicialSet.StdSimplexOne
public import Mathlib.AlgebraicTopology.SimplicialSet.Nonsingular

/-!
# Binary products `Δ[n] ⊗ Δ[1]`

In this file, we define a bijection `SSet.prodStdSimplex₁.nonDegenerateEquiv`
between `Fin (p + 1)` and the type of nondegenerate `(p + 1)`-simplices
of `Δ[p] ⊗ Δ[1]`.

-/

@[expose] public section

universe u

open CategoryTheory Simplicial MonoidalCategory Opposite
  CartesianMonoidalCategory

namespace SSet

namespace prodStdSimplex₁

variable {p : ℕ}

set_option backward.defeqAttrib.useBackward true in
open stdSimplex prodStdSimplex in
/-- This is an enumeration of the `p + 1` nondegenerate dimension-`(p + 1)`
simplices of `Δ[p] ⊗ Δ[1]`. It sends `i : Fin (p + 1)` to the nondegenerate
simplex consisting of the vertices
`(0, 0) ≤ (1,0) ≤ ... ≤ (i, 0) ≤ (i, 1) ≤ ... ≤ (p, 1)`. -/
noncomputable def nonDegenerateEquiv :
    Fin (p + 1) ≃ (Δ[p] ⊗ Δ[1] : SSet.{u}).nonDegenerate (p + 1) :=
  Equiv.ofBijective
    (fun i ↦ ⟨⟨stdSimplex.objEquiv.{u}.symm (SimplexCategory.σ i),
      objMk₁ i.succ.castSucc⟩, by
      rw [nonDegenerate_max_dim_iff _ rfl]
      ext j
      dsimp
      by_cases hj : j ≤ i.castSucc
      · rw [objMk₁_of_castSucc_lt _ _ (by simpa),
          Fin.coe_ofNat_eq_mod, Nat.zero_mod, add_zero]
        change (i.predAbove j : ℕ) = _
        simp [Fin.predAbove_of_le_castSucc _ _ hj]
      · simp only [not_le] at hj
        rw [objMk₁_of_le_castSucc _ _ (by simpa), objEquiv_symm_apply]
        change (i.predAbove j : ℕ) + 1 = _
        rw [Fin.predAbove_of_castSucc_lt _ _ hj, Fin.val_pred]
        lia⟩) (by
    refine ⟨fun _ _ h ↦ ?_, fun ⟨⟨s₁, s₂⟩, hs⟩ ↦ ?_⟩
    · simpa using stdSimplex.objMk₁_injective (congr_arg (Prod.snd ∘ Subtype.val) h)
    · rw [nonDegenerate_max_dim_iff _ rfl] at hs
      obtain ⟨i, rfl⟩ := stdSimplex.objMk₁_surjective s₂
      obtain ⟨i, rfl⟩ := Fin.eq_succ_of_ne_zero (i := i) (by
        rintro rfl
        have := DFunLike.congr_fun hs 0
        simp only [orderHomOfSimplex_coe,
          stdSimplex.objMk₁_of_le_castSucc (0 : Fin (p + 3)) 0 (by simp)] at this
        simp at this)
      obtain ⟨i, rfl⟩ | rfl := i.eq_castSucc_or_eq_last
      · exact ⟨i, nonDegenerate_ext₂ rfl rfl⟩
      · have := DFunLike.congr_fun hs (Fin.last _)
        simp only [Fin.succ_last, orderHomOfSimplex_coe,
          OrderHom.id_coe, id_eq, Fin.ext_iff, Fin.val_last,
          stdSimplex.objMk₁_of_castSucc_lt (Fin.last (p + 2))
            (Fin.last (p + 1)) (by simp),
          Fin.coe_ofNat_eq_mod, Nat.zero_mod, add_zero] at this
        lia)

@[simp]
lemma nonDegenerateEquiv_fst (i : Fin (p + 1)) :
    dsimp% (nonDegenerateEquiv i).val.1 =
      (stdSimplex.objEquiv (m := op ⦋p + 1⦌)).symm (SimplexCategory.σ i) := rfl

@[simp]
lemma nonDegenerateEquiv_snd (i : Fin (p + 1)) :
    dsimp% (nonDegenerateEquiv i).val.2 =
      stdSimplex.objMk₁ i.succ.castSucc := rfl

lemma mem_range_objEquiv_nonDegenerateEquiv₀_iff (i x : Fin (p + 1)) :
    dsimp% (x, 0) ∈ Set.range (prodStdSimplex.objEquiv (nonDegenerateEquiv i).val) ↔ x ≤ i := by
  constructor
  · rintro ⟨y, hy⟩
    have hy₁ := congr_arg Prod.fst hy
    have hy₂ := congr_arg Prod.snd hy
    dsimp at hy₁ hy₂
    rw [stdSimplex.objMk₁_apply_eq_zero_iff, Fin.succ_castSucc,
      Fin.castSucc_lt_castSucc_iff] at hy₂
    rw [← hy₁, stdSimplex.objEquiv_symm_σ_apply,
      Fin.predAbove_of_lt_succ _ _ hy₂, ← Fin.castSucc_le_castSucc_iff,
      Fin.castSucc_castPred, Fin.le_castSucc_iff]
    exact hy₂
  · refine fun hx ↦ ⟨x.castSucc, Prod.ext ?_ ?_⟩
    · dsimp
      rw [stdSimplex.objEquiv_symm_σ_apply,
        Fin.predAbove_of_le_castSucc _ _ (by simpa),
        Fin.castPred_castSucc]
    · simpa [stdSimplex.objMk₁_apply_eq_zero_iff]

lemma mem_range_objEquiv_nonDegenerateEquiv₁_iff (i x : Fin (p + 1)) :
    dsimp% (x, 1) ∈ Set.range (prodStdSimplex.objEquiv (nonDegenerateEquiv i).val) ↔ i ≤ x := by
  constructor
  · intro ⟨y, hy⟩
    have hy₁ := congr_arg Prod.fst hy
    have hy₂ := congr_arg Prod.snd hy
    dsimp at hy₁ hy₂
    rw [stdSimplex.objMk₁_apply_eq_one_iff, Fin.succ_castSucc, Fin.castSucc_le_castSucc_iff] at hy₂
    rw [← hy₁, stdSimplex.objEquiv_symm_σ_apply, Fin.predAbove_of_succ_le _ _ hy₂,
      ← Fin.succ_le_succ_iff, Fin.succ_pred]
    exact hy₂
  · refine fun hx ↦ ⟨x.succ, Prod.ext ?_ ?_⟩
    · dsimp
      rw [stdSimplex.objEquiv_symm_σ_apply,
        Fin.predAbove_of_succ_le _ _ (by simpa), Fin.pred_succ]
    · simpa [stdSimplex.objMk₁_apply_eq_one_iff]

/-- The filtration of `Δ[p] ⊗ Δ[1]` which sends `j : Fin (p + 1)` to the subcomplex
generated by the non-degenerate `(p + 1)`-simplices `nonDegenerateEquiv i` for `i ≤ j`. -/
noncomputable def filtration (j : Fin (p + 1)) : (Δ[p] ⊗ Δ[1] : SSet.{u}).Subcomplex :=
  ⨆ (i : ({i // i ≤ j} : Type)), .ofSimplex (nonDegenerateEquiv i.val).val

lemma ofSimplex_le_filtration {i j : Fin (p + 1)} (hij : i ≤ j) :
    .ofSimplex (nonDegenerateEquiv i).1 ≤ filtration.{u} j :=
  le_iSup (fun (i : {i // i ≤ j}) ↦
    Subcomplex.ofSimplex (nonDegenerateEquiv i.1).1) ⟨i, hij⟩

variable (p) in
lemma filtration_zero :
    filtration.{u} (0 : Fin (p + 1)) = .ofSimplex (nonDegenerateEquiv 0).val :=
  le_antisymm
    (by simpa [filtration] using Subcomplex.mem_ofSimplex_obj (nonDegenerateEquiv 0).val)
    (ofSimplex_le_filtration.{u} (by rfl))

variable (p) in
lemma filtration_last :
    filtration.{u} (Fin.last p) = ⊤ := by
  rw [prodStdSimplex.subcomplex_eq_top_iff _ rfl]
  intro x hx
  obtain ⟨i, hi⟩ := nonDegenerateEquiv.surjective ⟨x, hx⟩
  rw [Subtype.ext_iff] at hi
  subst hi
  rw [← Subcomplex.ofSimplex_le_iff]
  exact ofSimplex_le_filtration i.le_last

lemma filtration_monotone :
    Monotone (filtration.{u} (p := p)) := by
  intro j j' h
  rw [filtration, iSup_le_iff, Subtype.forall]
  intro i hi
  exact ofSimplex_le_filtration (hi.trans h)

/-- Given `i` and `j` in `Fin (p + 1)`, this is the subcomplex of `Δ[p] ⊗ Δ[1]`
obtained as the intersection of the subcomplexes generated by the
`i`th and `j`th `p + 1`-dimensional non-degenerate simplices given by
the enumeration `prodStdSimplex₁.nonDegenerateEquiv`. -/
noncomputable def intersectionNondeg (i j : Fin (p + 1)) :
    (Δ[p] ⊗ Δ[1] : SSet.{u}).Subcomplex :=
  .ofSimplex (nonDegenerateEquiv i).val ⊓ .ofSimplex (nonDegenerateEquiv j).val

/-- Given `j : Fin (p + 2)`, this is the non-degenerate `p`-simplex of `Δ[p] ⊗ Δ[1]`
which correspond to the stricly monotone map `Fin (p + 1) → Fin (p + 1) × Fin 2`
whose first projection is the identity, and whose second projection takes `j`-times
the value `0`. -/
@[simps coe_fst coe_snd]
def codimOneSimplex (j : Fin (p + 2)) : (Δ[p] ⊗ Δ[1] : SSet.{u}).nonDegenerate p :=
  ⟨⟨stdSimplex.objMk .id, stdSimplex.objMk₁ j⟩,
    nonDegenerate_of_app_apply (fst _ _)
      (stdSimplex.objEquiv_symm_id_mem_nonDegenerate p)⟩

set_option backward.defeqAttrib.useBackward true in
lemma δ_castSucc_nonDegenerateEquiv (j : Fin (p + 1)) :
    (Δ[p] ⊗ Δ[1]).δ j.castSucc (nonDegenerateEquiv.{u} j).val =
      (codimOneSimplex j.castSucc).val := by
  apply Prod.ext
  · exact stdSimplex.objEquiv.injective SimplexCategory.δ_comp_σ_self
  · dsimp
    rw [stdSimplex.δ_objMk₁_of_lt _ _ (by simp), Fin.pred_succ]

set_option backward.defeqAttrib.useBackward true in
lemma δ_succ_nonDegenerateEquiv (j : Fin (p + 1)) :
    (Δ[p] ⊗ Δ[1]).δ j.succ (nonDegenerateEquiv.{u} j).val =
      (codimOneSimplex j.succ).val := by
  apply Prod.ext
  · exact stdSimplex.objEquiv.injective SimplexCategory.δ_comp_σ_succ
  · dsimp
    rw [stdSimplex.δ_objMk₁_of_le _ _ (by simp)]
    simp only [Fin.succ_castSucc, Fin.castPred_castSucc]

set_option backward.defeqAttrib.useBackward true in
lemma ofSimplex_codimOneSimplex (j : Fin p) :
    Subcomplex.ofSimplex (codimOneSimplex.{u} j.succ.castSucc).val =
      intersectionNondeg j.castSucc j.succ := by
  apply le_antisymm
  · dsimp [intersectionNondeg]
    simp only [le_inf_iff, Subcomplex.ofSimplex_le_iff,
      Subcomplex.mem_ofSimplex_obj_iff]
    constructor
    · refine ⟨SimplexCategory.δ j.succ.castSucc, ?_⟩
      rw [← Fin.succ_castSucc, ← δ_succ_nonDegenerateEquiv]
      rfl
    · refine ⟨SimplexCategory.δ j.succ.castSucc, ?_⟩
      rw [Fin.succ_castSucc, ← δ_castSucc_nonDegenerateEquiv]
      rfl
  · rintro ⟨⟨k⟩⟩ s hs
    obtain ⟨f, rfl⟩ := prodStdSimplex.objEquiv.symm.surjective s
    simp only [prodStdSimplex.mem_ofSimplex_obj_iff, intersectionNondeg,
      Subfunctor.min_obj, Set.mem_inter_iff, Equiv.apply_symm_apply] at hs ⊢
    rintro ⟨x, y⟩ hxy
    refine ⟨x, Prod.ext rfl ?_⟩
    obtain ⟨x₁, h₁⟩ := hs.1 hxy
    obtain ⟨x₂, h₂⟩ := hs.2 hxy
    have h₁₁ := congr_arg Prod.fst h₁
    have h₁₂ := congr_arg Prod.snd h₁
    have h₂₁ := congr_arg Prod.fst h₂
    have h₂₂ := congr_arg Prod.snd h₂
    dsimp at h₁₁ h₁₂ h₂₁ h₂₂ ⊢
    rw [stdSimplex.objEquiv_symm_σ_apply] at h₁₁ h₂₁
    fin_cases y
    · dsimp at h₁₂ h₂₂ ⊢
      simp only [stdSimplex.objMk₁_apply_eq_zero_iff, ← Fin.le_castSucc_iff,
        Fin.castSucc_le_castSucc_iff, Fin.castSucc_succ] at h₁₂ h₂₂ ⊢
      rw [Fin.predAbove_of_le_castSucc _ _ h₁₂] at h₁₁
      simpa [← h₁₁] using! h₁₂
    · dsimp at h₁₂ h₂₂ ⊢
      simp only [stdSimplex.objMk₁_apply_eq_one_iff, Fin.succ_le_castSucc_iff] at h₁₂ h₂₂ ⊢
      rw [Fin.predAbove_of_castSucc_lt _ _ h₂₂] at h₂₁
      rwa [← h₂₁, ← Fin.succ_lt_succ_iff, Fin.succ_pred]

set_option backward.isDefEq.respectTransparency false in
set_option backward.defeqAttrib.useBackward true in
lemma intersectionNondeg_le_intersectionNondeg (i j k : Fin (p + 1))
    (hij : i ≤ j) (hij : j ≤ k) :
    intersectionNondeg.{u} i k ≤ intersectionNondeg j k := by
  rintro ⟨⟨k⟩⟩ x hx
  dsimp [intersectionNondeg] at hx ⊢
  simp only [Set.mem_inter_iff, prodStdSimplex.mem_ofSimplex_obj_iff.{u},
    ← Set.subset_inter_iff] at hx ⊢
  refine hx.trans (fun (x, y) hxy ↦ ?_)
  simp only [Set.mem_inter_iff] at hxy ⊢
  fin_cases y
  all_goals
  · dsimp at hxy ⊢
    simp only [mem_range_objEquiv_nonDegenerateEquiv₀_iff,
      mem_range_objEquiv_nonDegenerateEquiv₁_iff] at hxy ⊢
    lia

set_option backward.isDefEq.respectTransparency false in
set_option backward.defeqAttrib.useBackward true in
lemma intersectionNondeg_le_intersectionNondeg' (i j k : Fin (p + 1))
    (hij : i ≤ j) (hij : j ≤ k) :
    intersectionNondeg.{u} i k ≤ intersectionNondeg i j := by
  rintro ⟨⟨k⟩⟩ x hx
  dsimp [intersectionNondeg] at hx ⊢
  simp only [Set.mem_inter_iff, prodStdSimplex.mem_ofSimplex_obj_iff.{u},
    ← Set.subset_inter_iff] at hx ⊢
  refine hx.trans (fun (x, y) hxy ↦ ?_)
  simp only [Set.mem_inter_iff] at hxy ⊢
  fin_cases y
  all_goals
  · dsimp at hxy ⊢
    simp only [mem_range_objEquiv_nonDegenerateEquiv₀_iff,
      mem_range_objEquiv_nonDegenerateEquiv₁_iff] at hxy ⊢
    lia

lemma filtration.bicartSq (j : Fin p) :
    Subcomplex.BicartSq (Subcomplex.ofSimplex (codimOneSimplex.{u} j.succ.castSucc).1)
      (filtration.{u} j.castSucc) (.ofSimplex (nonDegenerateEquiv j.succ).1)
      (filtration.{u} j.succ) where
  inf_eq := by
    rw [ofSimplex_codimOneSimplex]
    apply le_antisymm
    · dsimp [filtration]
      rw [Subfunctor.iSup_min, iSup_le_iff, Subtype.forall]
      intro i hi
      exact intersectionNondeg_le_intersectionNondeg _ _ _ hi j.castSucc_le_succ
    · dsimp [intersectionNondeg]
      simp only [le_inf_iff, inf_le_right, and_true]
      exact inf_le_left.trans (ofSimplex_le_filtration (by rfl))
  sup_eq := by
    dsimp [filtration]
    refine le_antisymm ?_ ?_
    · rw [sup_le_iff, iSup_le_iff, Subtype.forall]
      refine ⟨fun i hi ↦ ?_, ?_⟩
      · exact ofSimplex_le_filtration (hi.trans (j.castSucc_le_succ))
      · exact ofSimplex_le_filtration (by rfl)
    · rw [iSup_le_iff, Subtype.forall]
      intro i hi
      obtain hi | rfl := hi.lt_or_eq
      · refine le_trans ?_ le_sup_left
        exact ofSimplex_le_filtration (Fin.le_castSucc_iff.2 hi)
      · exact le_sup_right

/-- Assuming the inequality `i ≤ j` in `Fin (p + 1)`, this is
the inclusion `Δ[p + 1] ⟶ filtration.{u} j` of the `i`th
non-degenerate `(p + 1)`-simplex in `Δ[p] ⊗ Δ[1]` of the
enumeration `nonDegenerateEquiv`. -/
noncomputable def filtration.ι {i j : Fin (p + 1)} (hi : i ≤ j) :
    Δ[p + 1] ⟶ filtration.{u} j :=
  Subcomplex.lift
    (yonedaEquiv.symm (nonDegenerateEquiv i)) (by
      rw [Subcomplex.range_eq_ofSimplex, Equiv.apply_symm_apply]
      exact ofSimplex_le_filtration hi)

@[reassoc (attr := simp)]
lemma filtration.ι_ι (i j : Fin (p + 1)) (hi : i ≤ j) :
    filtration.ι.{u} hi ≫ (filtration j).ι =
      yonedaEquiv.symm (nonDegenerateEquiv i).val := rfl

set_option backward.defeqAttrib.useBackward true in
/-- Given `j : Fin p`, the subcomplex `filtration j.succ` of `Δ[p] ⊗ Δ[1]`
is the pushout of `filtration j` and of the inclusion
`filtration.ι j.succ : Δ[p + 1] ⟶ (filtration j.succ).toSSet`
along the `j.succ.castSucc`th face of `Δ[p + 1]`. -/
lemma filtration.isPushout (j : Fin p) :
    IsPushout
      (stdSimplex.{u}.δ j.succ.castSucc ≫ ι (le_refl j.castSucc))
      (stdSimplex.δ j.succ.castSucc)
      (Subcomplex.homOfLE (filtration_monotone j.castSucc_le_succ))
      (ι (le_refl j.succ)) := by
  fapply (filtration.bicartSq.{u} j).isPushout.of_iso'
    (stdSimplex.isoOfRepresentableBy
      (Subcomplex.ofSimplexRepresentableBy _)) (Iso.refl _)
    (stdSimplex.isoOfRepresentableBy
      (Subcomplex.ofSimplexRepresentableBy _)) (Iso.refl _)
  · ext : 1
    dsimp
    simp only [Category.assoc, Category.comp_id, ι_ι]
    rw [Subcomplex.homOfLE_ι,
      Subcomplex.isoOfRepresentableBy_ofSimplexRepresentableBy_hom]
    dsimp [CosimplicialObject.δ]
    rw [← dsimp% yonedaEquiv_symm_map (X := _ ⊗ _),
      ← δ_succ_nonDegenerateEquiv]
    rfl
  · ext : 1
    dsimp
    simp only [Category.assoc]
    rw [Subcomplex.homOfLE_ι,
      Subcomplex.isoOfRepresentableBy_ofSimplexRepresentableBy_hom,
      Subcomplex.isoOfRepresentableBy_ofSimplexRepresentableBy_hom]
    dsimp [CosimplicialObject.δ]
    rw [← dsimp% yonedaEquiv_symm_map (X := _ ⊗ _),
      Fin.succ_castSucc, ← δ_castSucc_nonDegenerateEquiv]
    rfl
  · simp
  · cat_disch

end prodStdSimplex₁

@[deprecated (since := "2026-07-12")]
alias prodStdSimplex.nonDegenerateEquiv₁ := prodStdSimplex₁.nonDegenerateEquiv
@[deprecated (since := "2026-07-12")]
alias prodStdSimplex.nonDegenerateEquiv₁_fst := prodStdSimplex₁.nonDegenerateEquiv_fst
@[deprecated (since := "2026-07-12")]
alias prodStdSimplex.nonDegenerateEquiv₂_snd := prodStdSimplex₁.nonDegenerateEquiv_snd

end SSet
