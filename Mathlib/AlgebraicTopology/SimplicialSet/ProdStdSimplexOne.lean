/-
Copyright (c) 2025 Joël Riou. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
Authors: Joël Riou
-/
module

public import Mathlib.AlgebraicTopology.SimplicialSet.ProdStdSimplex
public import Mathlib.AlgebraicTopology.SimplicialSet.StdSimplexOne

/-!
# Binary products `Δ[n] ⊗ Δ[1]`

In this file, we define a bijection `SSet.prodStdSimplex₁.nonDegenerateEquiv`
between `Fin (p + 1)` and the type of nondegenerate `(p + 1)`-simplices
of `Δ[p] ⊗ Δ[1]`.

-/

@[expose] public section

universe u

open CategoryTheory Simplicial MonoidalCategory Opposite
  CartesianMonoidalCategory

namespace SSet

namespace prodStdSimplex₁

variable {p : ℕ}

set_option backward.defeqAttrib.useBackward true in
open stdSimplex prodStdSimplex in
/-- This is an enumeration of the `p + 1` nondegenerate dimension-`(p + 1)`
simplices of `Δ[p] ⊗ Δ[1]`. It sends `i : Fin (p + 1)` to the nondegenerate
simplex consisting of the vertices
`(0, 0) ≤ (1,0) ≤ ... ≤ (i, 0) ≤ (i, 1) ≤ ... ≤ (p, 1)`. -/
noncomputable def nonDegenerateEquiv :
    Fin (p + 1) ≃ (Δ[p] ⊗ Δ[1] : SSet.{u}).nonDegenerate (p + 1) :=
  Equiv.ofBijective
    (fun i ↦ ⟨⟨stdSimplex.objEquiv.{u}.symm (SimplexCategory.σ i),
      objMk₁ i.succ.castSucc⟩, by
      rw [nonDegenerate_max_dim_iff _ rfl]
      ext j
      dsimp
      by_cases hj : j ≤ i.castSucc
      · rw [objMk₁_of_castSucc_lt _ _ (by simpa),
          Fin.coe_ofNat_eq_mod, Nat.zero_mod, add_zero]
        change (i.predAbove j : ℕ) = _
        simp [Fin.predAbove_of_le_castSucc _ _ hj]
      · simp only [not_le] at hj
        rw [objMk₁_of_le_castSucc _ _ (by simpa), objEquiv_symm_apply]
        change (i.predAbove j : ℕ) + 1 = _
        rw [Fin.predAbove_of_castSucc_lt _ _ hj, Fin.val_pred]
        lia⟩) (by
    refine ⟨fun _ _ h ↦ ?_, fun ⟨⟨s₁, s₂⟩, hs⟩ ↦ ?_⟩
    · simpa using stdSimplex.objMk₁_injective (congr_arg (Prod.snd ∘ Subtype.val) h)
    · rw [nonDegenerate_max_dim_iff _ rfl] at hs
      obtain ⟨i, rfl⟩ := stdSimplex.objMk₁_surjective s₂
      obtain ⟨i, rfl⟩ := Fin.eq_succ_of_ne_zero (i := i) (by
        rintro rfl
        have := DFunLike.congr_fun hs 0
        simp only [orderHomOfSimplex_coe,
          stdSimplex.objMk₁_of_le_castSucc (0 : Fin (p + 3)) 0 (by simp)] at this
        simp at this)
      obtain ⟨i, rfl⟩ | rfl := i.eq_castSucc_or_eq_last
      · exact ⟨i, nonDegenerate_ext₂ rfl rfl⟩
      · have := DFunLike.congr_fun hs (Fin.last _)
        simp only [Fin.succ_last, orderHomOfSimplex_coe,
          OrderHom.id_coe, id_eq, Fin.ext_iff, Fin.val_last,
          stdSimplex.objMk₁_of_castSucc_lt (Fin.last (p + 2))
            (Fin.last (p + 1)) (by simp),
          Fin.coe_ofNat_eq_mod, Nat.zero_mod, add_zero] at this
        lia)

@[simp]
lemma nonDegenerateEquiv_fst (i : Fin (p + 1)) :
    dsimp% (nonDegenerateEquiv i).val.1 =
      (stdSimplex.objEquiv (m := op ⦋p + 1⦌)).symm (SimplexCategory.σ i) := rfl

@[simp]
lemma nonDegenerateEquiv_snd (i : Fin (p + 1)) :
    dsimp% (nonDegenerateEquiv i).val.2 =
      stdSimplex.objMk₁ i.succ.castSucc := rfl

/-- The filtration of `Δ[p] ⊗ Δ[1]` which sends `j : Fin (p + 1)` to the subcomplex
generated by the non-degenerate `(p + 1)`-simplices `nonDegenerateEquiv i` for `i ≤ j`. -/
noncomputable def filtration (j : Fin (p + 1)) : (Δ[p] ⊗ Δ[1] : SSet.{u}).Subcomplex :=
  ⨆ (i : ({i // i ≤ j} : Type)), .ofSimplex (nonDegenerateEquiv i.val).val

lemma ofSimplex_le_filtration {i j : Fin (p + 1)} (hij : i ≤ j) :
    .ofSimplex (nonDegenerateEquiv i).1 ≤ filtration.{u} j :=
  le_iSup (fun (i : {i // i ≤ j}) ↦
    Subcomplex.ofSimplex (nonDegenerateEquiv i.1).1) ⟨i, hij⟩

variable (p) in
lemma filtration_zero :
    filtration.{u} (0 : Fin (p + 1)) = .ofSimplex (nonDegenerateEquiv 0).val :=
  le_antisymm
    (by simpa [filtration] using Subcomplex.mem_ofSimplex_obj (nonDegenerateEquiv 0).val)
    (ofSimplex_le_filtration.{u} (by rfl))

variable (p) in
lemma filtration_last :
    filtration.{u} (Fin.last p) = ⊤ := by
  rw [prodStdSimplex.subcomplex_eq_top_iff _ rfl]
  intro x hx
  obtain ⟨i, hi⟩ := nonDegenerateEquiv.surjective ⟨x, hx⟩
  rw [Subtype.ext_iff] at hi
  subst hi
  rw [← Subcomplex.ofSimplex_le_iff]
  exact ofSimplex_le_filtration i.le_last

lemma filtration_monotone :
    Monotone (filtration.{u} (p := p)) := by
  intro j j' h
  rw [filtration, iSup_le_iff, Subtype.forall]
  intro i hi
  exact ofSimplex_le_filtration (hi.trans h)

/-- Given `i` and `j` in `Fin (p + 1)`, this is the subcomplex of `Δ[p] ⊗ Δ[1]`
obtained as the intersection of the subcomplexes generated by the
`i`th and `j`th `p + 1`-codimensional non-degenerate simplices given by
the enumeration `prodStdSimplex₁.nonDegenerateEquiv`. -/
noncomputable abbrev intersectionNondeg (i j : Fin (p + 1)) :
    (Δ[p] ⊗ Δ[1] : SSet.{u}).Subcomplex :=
  .ofSimplex (nonDegenerateEquiv i).1 ⊓ .ofSimplex (nonDegenerateEquiv j).1

/-- Given `j : Fin (p + 2)`, this is the non-degenerate `p`-simplex of `Δ[p] ⊗ Δ[1]`
which correspond to the stricly monotone map `Fin (p + 1) → Fin (p + 1) × Fin 2`
whose first projection is the identity, and whose second projection takes `j`-times
the value `0`. -/
@[simps coe_fst coe_snd]
def codimOneSimplex (j : Fin (p + 2)) : (Δ[p] ⊗ Δ[1] : SSet.{u}).nonDegenerate p :=
  ⟨⟨stdSimplex.objMk .id, stdSimplex.objMk₁ j⟩,
    nonDegenerate_of_app_apply (fst _ _)
      (stdSimplex.objEquiv_symm_id_mem_nonDegenerate p)⟩

set_option backward.defeqAttrib.useBackward true in
lemma δ_castSucc_nonDegenerateEquiv (j : Fin (p + 1)) :
    (Δ[p] ⊗ Δ[1]).δ j.castSucc (nonDegenerateEquiv.{u} j).1 =
      (codimOneSimplex j.castSucc).1 := by
  apply Prod.ext
  · exact stdSimplex.objEquiv.injective SimplexCategory.δ_comp_σ_self
  · dsimp
    rw [stdSimplex.δ_objMk₁_of_lt _ _ (by simp), Fin.pred_succ]

set_option backward.defeqAttrib.useBackward true in
lemma δ_succ_nonDegenerateEquiv (j : Fin (p + 1)) :
    (Δ[p] ⊗ Δ[1]).δ j.succ (nonDegenerateEquiv.{u} j).1 =
      (codimOneSimplex j.succ).1 := by
  apply Prod.ext
  · exact stdSimplex.objEquiv.injective SimplexCategory.δ_comp_σ_succ
  · dsimp
    rw [stdSimplex.δ_objMk₁_of_le _ _ (by simp)]
    simp only [Fin.succ_castSucc, Fin.castPred_castSucc]

end prodStdSimplex₁

@[deprecated (since := "2026-07-12")]
alias prodStdSimplex.nonDegenerateEquiv₁ := prodStdSimplex₁.nonDegenerateEquiv
@[deprecated (since := "2026-07-12")]
alias prodStdSimplex.nonDegenerateEquiv₁_fst := prodStdSimplex₁.nonDegenerateEquiv_fst
@[deprecated (since := "2026-07-12")]
alias prodStdSimplex.nonDegenerateEquiv₂_snd := prodStdSimplex₁.nonDegenerateEquiv_snd

end SSet
